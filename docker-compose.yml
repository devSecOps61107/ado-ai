version: '3.8'

services:
  api:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    environment:
      - JWT_SECRET=your-secret-key-here  # Change this in production
      - ENVIRONMENT=production